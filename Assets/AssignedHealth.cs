using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AssignedHealth : MonoBehaviour
{
    // Start is called before the first frame update

    public int health = 600;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public int GetMax()
    {
        return health;
    }
}
